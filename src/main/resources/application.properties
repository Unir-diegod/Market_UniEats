# ===============================================================
# CONFIGURACIÓN LOCAL CON SUPABASE POSTGRESQL
# ===============================================================

# --- Configuración del Servidor ---
server.port=8092

# --- Conexión a la Base de Datos Supabase PostgreSQL ---
spring.datasource.url=jdbc:postgresql://db.lfvweearttrisbbhemld.supabase.co:5432/postgres
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=F1001504182.ae

# --- Configuración de Hibernate (JPA) ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# --- Configuración de Almacenamiento Local ---
# Directorio local para guardar imágenes
app.upload.directory=./uploads

# --- Configuración del Servicio de Correo ---
# Configuración de Gmail SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dvdavid2509vargs@gmail.com
spring.mail.password=qiws yptl afef tzmq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Configuración adicional de email
app.mail.from=dvdavid2509vargs@gmail.com
app.mail.from-name=UniEats Marketplace

# --- Configuración para archivos multipart ---
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# --- Configuración de Logging ---
logging.level.com.remington.unieats=INFO
logging.level.org.hibernate.SQL=WARN   